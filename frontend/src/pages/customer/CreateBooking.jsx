import { useMemo, useState } from 'react'
import { useNavigate, useParams } from 'react-router-dom'
import { CalendarPlus } from 'lucide-react'
import { brand } from '../../data/brandData'
import { useAuth } from '../../hooks/useAuth'
import { saveStoredOrder } from '../../data/mockOrders'
import { rupiah } from '../../lib/utils'
import { Card, EmptyState, SectionHeader } from '../../components/ui'

const demoPackages = [{ id:'1', name:'Wedding Cinematic Highlight', category:'Wedding', price:4500000, description:'Cinematic wedding coverage for intimate traditional ceremonies and warm family moments.', deliverables:['1 cinematic highlight film','Edited photo selection','Color grading','Online delivery gallery'] }]
export default function CreateBooking(){ const { packageId }=useParams(); const { user }=useAuth(); const navigate=useNavigate(); const pkg=useMemo(()=>demoPackages.find(p=>p.id===String(packageId)),[packageId]); const [success,setSuccess]=useState(false); const [form,setForm]=useState({eventType:user.preferredEventType||'Wedding Cinematic Highlight',eventDate:'',eventLocation:user.location||'Bali, Indonesia',notes:user.notes||'',whatsapp:user.whatsapp||''}); if(!pkg) return <section className="section-pad"><div className="container-premium"><EmptyState title="Package not found" text="Paket demo ini belum tersedia. Pilih paket lain dari katalog."/></div></section>; const set=(k,v)=>setForm({...form,[k]:v}); function submit(e){e.preventDefault(); const code=`MLG-DEMO-${Date.now().toString().slice(-6)}`; saveStoredOrder({ id:`order-${code}`, kode:code, kode_pemesanan:code, invoiceCode:null, customerEmail:user.email, customerName:user.name, packageId:pkg.id, packageName:pkg.name, nama_paket:pkg.name, eventType:form.eventType, eventDate:form.eventDate || 'Tanggal menyusul', tanggal_acara:form.eventDate, eventLocation:form.eventLocation, paymentStatus:'Waiting Confirmation', productionStatus:'Booking Created', editingProgress:0, progress:0, editorName:'Mellogang Team', total_biaya:pkg.price, notes:form.notes, timeline:[{label:'Booking Created', date:'Just now', status:'active'},{label:'DP Uploaded', date:'Pending', status:'pending'},{label:'DP Verified', date:'Pending', status:'pending'}]}); setSuccess(true); setTimeout(()=>navigate('/status'),1100) } return <section className="section-pad"><div className="container-premium"><SectionHeader eyebrow="Create Booking" title="Shape your moment into a production plan">Package ID {pkg.id} is ready in demo mode.</SectionHeader><div className="grid gap-6 lg:grid-cols-[.85fr_1.15fr]"><Card><p className="eyebrow">{pkg.category}</p><h2 className="mt-4 text-3xl font-semibold text-cream light:text-charcoal">{pkg.name}</h2><p className="subtle mt-3">{pkg.description}</p><p className="mt-6 text-3xl font-semibold text-gold">{rupiah(pkg.price)}</p><ul className="mt-6 space-y-3 text-sm text-cream/65 light:text-black/60">{pkg.deliverables.map(d=><li key={d}>• {d}</li>)}</ul></Card><Card><form onSubmit={submit} className="grid gap-4 md:grid-cols-2"><input className="input-dark" value={form.eventType} onChange={e=>set('eventType',e.target.value)} placeholder="Event type"/><input className="input-dark" type="date" value={form.eventDate} onChange={e=>set('eventDate',e.target.value)}/><input className="input-dark" value={form.eventLocation} onChange={e=>set('eventLocation',e.target.value)} placeholder="Event location"/><input className="input-dark" value={pkg.name} readOnly/><input className="input-dark md:col-span-2" value={form.whatsapp} onChange={e=>set('whatsapp',e.target.value)} placeholder="WhatsApp"/><textarea className="input-dark min-h-32 md:col-span-2" value={form.notes} onChange={e=>set('notes',e.target.value)} placeholder="Notes/concept"/>{success&&<div className="rounded-2xl border border-gold/20 bg-gold/10 p-3 text-sm text-cream md:col-span-2">Pesanan demo berhasil dibuat. Tim Mellogang akan menghubungi kamu untuk konfirmasi konsep.</div>}<button className="btn-primary"><CalendarPlus size={16}/>Buat Pesanan</button><a className="btn-secondary" href={brand.whatsapp} target="_blank" rel="noreferrer">Booking via WhatsApp</a></form></Card></div></div></section> }
