import { Link } from 'react-router-dom'
import { Edit3 } from 'lucide-react'
import { useAuth } from '../hooks/useAuth'
import { Card, SectionHeader } from '../components/ui'

export default function Profile(){ const { user } = useAuth(); return <section className="section-pad"><div className="container-premium max-w-4xl"><SectionHeader eyebrow="Profile" title="Your Mellogang client profile">Data demo ini dipakai untuk prefill booking, status pesanan, dan invoice.</SectionHeader><Card><div className="flex flex-col gap-6 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><span className="grid h-16 w-16 place-items-center rounded-full bg-gold text-xl font-bold text-black">{user.avatarInitials || 'DC'}</span><div><h1 className="text-3xl font-semibold text-cream">{user.name}</h1><p className="subtle">{user.email}</p></div></div><Link to="/profile/edit" className="btn-primary"><Edit3 size={16}/>Edit Profile</Link></div><div className="mt-8 grid gap-4 md:grid-cols-2">{[['WhatsApp',user.whatsapp],['Preferred Event',user.preferredEventType],['Location / City',user.location],['Notes / Concept',user.notes]].map(([k,v])=><div key={k} className="rounded-3xl border border-white/10 bg-white/5 p-5 light:border-black/10 light:bg-black/5"><p className="text-sm text-cream/45 light:text-black/45">{k}</p><p className="mt-2 font-semibold text-cream light:text-charcoal">{v || '-'}</p></div>)}</div></Card></div></section> }
