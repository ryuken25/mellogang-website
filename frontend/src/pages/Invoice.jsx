import { Printer } from 'lucide-react'
import { Card, SectionHeader, StatusBadge } from '../components/ui'
import { orders } from '../data/mockData'
import { rupiah } from '../lib/utils'
export default function Invoice(){const order=orders[0];return <section className="section-pad"><div className="container-premium max-w-5xl"><SectionHeader eyebrow="Invoice" title="Clean printable invoice layout">Layout invoice siap print/download. Untuk data real, gunakan URL backend /invoice/{kode}.</SectionHeader><Card className="bg-cream text-black print:shadow-none"><div className="flex flex-col gap-6 border-b border-black/10 pb-8 md:flex-row md:items-start md:justify-between"><div><p className="text-sm uppercase tracking-[.3em] text-black/50">Mellogang Visuals</p><h1 className="mt-3 text-4xl font-semibold">Invoice</h1><p className="mt-2 text-black/55">#{order.kode_pemesanan}</p></div><button onClick={()=>window.print()} className="inline-flex items-center gap-2 rounded-full bg-black px-5 py-3 text-sm font-semibold text-white print:hidden"><Printer size={16}/>Print / Download</button></div><div className="grid gap-8 py-8 md:grid-cols-2"><div><p className="text-black/45">Client</p><h3 className="text-2xl font-semibold">Pelanggan Mellogang</h3><p className="mt-2 text-black/55">Booking creative production</p></div><div className="md:text-right"><p className="text-black/45">Status</p><StatusBadge status={order.status_pemesanan}/><p className="mt-4 text-black/45">Tanggal acara: {order.tanggal_acara}</p></div></div><div className="overflow-hidden rounded-3xl border border-black/10"><table className="w-full text-left"><thead className="bg-black/5"><tr><th className="p-4">Paket</th><th className="p-4">Status Produksi</th><th className="p-4 text-right">Total</th></tr></thead><tbody><tr><td className="p-4">{order.nama_paket}</td><td className="p-4">{order.status_produksi}</td><td className="p-4 text-right font-semibold">{rupiah(order.total_biaya)}</td></tr></tbody></table></div><div className="mt-8 flex justify-end"><div className="w-full max-w-sm rounded-3xl bg-black p-6 text-white"><p className="text-white/50">Total Pembayaran</p><p className="mt-2 text-3xl font-semibold text-gold">{rupiah(order.total_biaya)}</p></div></div></Card></div></section>}
