import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { Save } from 'lucide-react'
import { useAuth } from '../hooks/useAuth'
import { Card, SectionHeader } from '../components/ui'

export default function EditProfile(){ const { user, updateProfile } = useAuth(); const navigate=useNavigate(); const [form,setForm]=useState({ name:user.name||'', email:user.email||'', whatsapp:user.whatsapp||'', preferredEventType:user.preferredEventType||'', location:user.location||'', notes:user.notes||'' }); const set=(k,v)=>setForm({...form,[k]:v}); function submit(e){e.preventDefault(); updateProfile({...form, avatarInitials: form.name.split(' ').map(x=>x[0]).slice(0,2).join('').toUpperCase() || user.avatarInitials}); navigate('/profile')} return <section className="section-pad"><div className="container-premium max-w-4xl"><SectionHeader eyebrow="Edit Profile" title="Refine your production details">Update demo profile. Changes persist in localStorage.</SectionHeader><Card><form onSubmit={submit} className="grid gap-4 md:grid-cols-2"><input className="input-dark" value={form.name} onChange={e=>set('name',e.target.value)} placeholder="Full name"/><input className="input-dark" type="email" value={form.email} onChange={e=>set('email',e.target.value)} placeholder="Email"/><input className="input-dark" value={form.whatsapp} onChange={e=>set('whatsapp',e.target.value)} placeholder="WhatsApp number"/><input className="input-dark" value={form.preferredEventType} onChange={e=>set('preferredEventType',e.target.value)} placeholder="Preferred event type"/><input className="input-dark md:col-span-2" value={form.location} onChange={e=>set('location',e.target.value)} placeholder="Location/city"/><textarea className="input-dark min-h-32 md:col-span-2" value={form.notes} onChange={e=>set('notes',e.target.value)} placeholder="Notes/concept preference"/><button className="btn-primary md:col-span-2"><Save size={16}/>Save Profile</button></form></Card></div></section> }
